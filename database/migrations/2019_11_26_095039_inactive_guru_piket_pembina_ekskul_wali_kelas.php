<?php

use Illuminate\Support\Facades\Schema;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Database\Migrations\Migration;

use App\Models\Modul;

class InactiveGuruPiketPembinaEkskulWaliKelas extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        // $modul = Modul::find(35);
        // $modul->akses = 0;
        // $modul->save();

        // $modul = Modul::find(36);
        // $modul->akses = 0;
        // $modul->save();

        // $modul = Modul::find(37);
        // $modul->akses = 0;
        // $modul->save();
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        //
    }
}
